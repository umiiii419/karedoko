<h4>ジャンル編集</h4>

<%= form_with model:@genre, url: admins_genre_path, local: true do |f| %>
  <%= f.label      :name, "ジャンル名" %>
  <%= f.text_field :name, placeholder: "ジャンル名" %>
  <%= f.submit "変更を保存" %>
<% end %>

